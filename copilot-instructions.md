# Copilot Instructions

このファイルは、`demo-atrs` リポジトリで GitHub Copilot を利用する際の実装ルールを定義する。

## 1. 目的
- 既存アーキテクチャに沿った安全な変更を行う
- 影響範囲を限定し、レビューしやすい差分を作る
- 日本語での明確な説明と、再現可能な検証結果を残す

## 2. 基本ポリシー
- 既存実装の意図を尊重し、最小変更で要件を満たす
- 指示されていない改善（大規模リファクタリング、命名変更、不要な最適化）は行わない
- 変更理由が分かる単位で実装し、必要最小限のファイルのみ編集する
- 既存の依存関係・ビルド設定を優先し、新規ライブラリ追加は必要性を明示する

## 3. コミュニケーション
- 返答は日本語で行う
- 実施内容は「何を」「どこに」「なぜ」を短く明記する
- 確認できたことと未確認事項（ローカル未実行など）を分けて記載する

## 4. リポジトリ構成の前提
本プロジェクトは Maven マルチモジュール構成。

- `atrs-domain`: ドメイン層（エンティティ、ビジネスロジック、ドメインサービス）
- `atrs-env`: インフラ・環境依存設定（Spring 設定、ログ設定など）
- `atrs-initdb`: DB 初期化 SQL
- `atrs-web`: Web 層（Controller、Form、View(JSP)、静的リソース）

## 5. 実装ルール

### 5.1 Java / Spring
- レイヤ責務を維持する（Web 層にビジネスロジックを持ち込まない）
- 既存の DI、設定、例外ハンドリング方針に合わせる
- 既存の命名規則・パッケージ構成を崩さない
- 既存で利用中のユーティリティや共通部品を優先再利用する

### 5.2 JSP / JavaScript / CSS
- 既存画面の構造・クラス設計・UI 挙動と整合するよう変更する
- `atrs-web/src/main/webapp/resources/js` 配下は既存ファイルの責務ごとに修正する
- スタイル変更は `style.css` など既存の管理方針に従い、過剰な UI 変更を避ける

### 5.3 メッセージ・バリデーション
- 文言は既存の `ValidationMessages.properties` / `i18n` リソースを優先利用する
- バリデーション追加時は、サーバー側とクライアント側の整合を意識する
- ハードコード文字列は極力避ける

### 5.4 SQL / DB
- SQL は `atrs-initdb/src/sqls` の命名規則と実行順序を維持する
- 既存データへの影響がある変更は、目的と移行方針を明記する
- Integration Test 用 SQL と実運用想定データを混在させない

## 6. 変更時の禁止事項
- 無関係ファイルの整形・リネーム・大量変更
- ライセンス表記の変更
- 認証情報、個人情報、秘密情報の生成・出力・埋め込み
- 根拠のない設定値変更（タイムアウト、ログレベル、DB 設定など）

## 7. 検証方針
- 変更箇所に最も近い単位から優先的に確認する
- 可能な場合は対象モジュール単位で Maven テストを実施する
- 実行不可の場合は、未実行理由と想定確認手順を明示する

例:
- `mvn -pl atrs-web test`
- `mvn -pl atrs-domain test`

## 8. 出力フォーマット（推奨）
作業完了時は以下を簡潔に示す。

1. 変更概要（1〜3 行）
2. 変更ファイル一覧
3. 検証結果（実行コマンドと結果）
4. 必要に応じた残課題・注意点

## 9. この指示の優先順位
- 最優先: ユーザーからの明示的な指示
- 次点: 本 `copilot-instructions.md`
- その次: 既存コードの実装規約・慣例

矛盾がある場合は、最小リスクとなる案を採用し、前提を明記する。
